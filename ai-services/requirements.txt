# Web Framework
fastapi
uvicorn[standard]
websockets

# Legacy Flask (keep for backward compatibility)
flask
flask-cors
gunicorn

# Image Processing
opencv-python
numpy

# AI/ML
mediapipe
ultralytics
insightface
onnxruntime

# Utilities
requests
python-dotenv

# Local interview-question generator (training + inference) - offline, no API key
torch
transformers
datasets
accelerate
peft

# Local tech-question dataset QA classifier (TF-IDF + Logistic Regression) - offline, no API key
scikit-learn
joblib

# Local speech (STT/TTS) - offline, no API key, no network call
faster-whisper
pyttsx3
pywin32; sys_platform == "win32"
